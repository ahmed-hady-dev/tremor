import 'package:flutter/material.dart';

class SognUpView extends StatelessWidget {
  const SognUpView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('SognUpView'),
        ),
      ),
    );
  }
}
