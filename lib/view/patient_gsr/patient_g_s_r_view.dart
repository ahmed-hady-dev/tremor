import 'package:flutter/material.dart';

class PatientGSRView extends StatelessWidget {
  const PatientGSRView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('PatientGSRView'),
        ),
      ),
    );
  }
}
